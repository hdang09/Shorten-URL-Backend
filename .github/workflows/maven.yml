# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Java CI with Maven

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      run: mvn clean install

    # Optional: Uploads the full dependency graph to GitHub to improve the quality of Dependabot alerts this repository can receive
    - name: Update dependency graph
      uses: advanced-security/maven-dependency-submission-action@571e99aab1055c2e71a1e2309b9691de18d6b7d6

    - name: Write value to Properties-file
      uses: christian-draeger/write-properties@1.1.0
      with:
       path: './src/main/resources/application.properties'
       property: |
        'spring.datasource.url'
        'spring.datasource.username'
        'spring.datasource.password'
        'server.port'
        'url.client'
        'url.host'
        'spring.security.oauth2.client.registration.google.client-id'
        'spring.security.oauth2.client.registration.google.client-secret'
        'spring.security.oauth2.client.registration.google.redirect-uri'
        'application.security.jwt.secret-key'
        'application.security.jwt.expiration'
        'application.security.jwt.refresh-token.expiration'
        'logging.level.org.springframework.security'

       value: |
        ${{ secrets.DATA_SOURCE }}
        ${{ secrets.DB_USERNAME }}
        ${{ secrets.DB_PASSWORD }}
        '8080'
        'http://localhost:3000'
        'https://shorten-api.hdang09.site'
        '351765774586-ci0tlifsmk38ti87nr384pa6uupj7dqa.apps.googleusercontent.com'
        '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'
        'https://shorten-api.hdang09.site/login/oauth2/code/google'
        '404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970'
        '86400000'
        '604800000'
        'TRACE'